# React form

Run : `npm run dev`
Launch Cypress:

- Launch the server: `npm run dev`
- `npx cypress open`

Enable code coverage

- Run the server with instrumented code: `npm run dev:coverage`
- Run cypress: `npx cypress open`
